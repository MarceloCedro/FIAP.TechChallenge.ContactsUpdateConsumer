
apiVersion: v1
kind: Service
metadata:
  name: contact-update-consumer-api-nodeport
spec:
  selector:
    app: contact-update-consumer-api
  ports:
    - protocol: TCP
      port: 80           # Porta acessada externamente
      targetPort: 3030   # Porta do container
      nodePort: 30086    # Porta exposta no n� (opcional)
  type: NodePort
---
# Service LoadBalancer: Acesso externo gerenciado (em nuvem)
apiVersion: v1
kind: Service
metadata:
  name: contact-update-consumer-api-lb
spec:
  selector:
    app: contact-update-consumer-api
  ports:
    - protocol: TCP
      port: 5002           # Porta do servi�o
      targetPort: 3030   # Porta do container
  type: LoadBalancer
